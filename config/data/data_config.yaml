# config.yaml

# -------------------------
# Base
# -------------------------
base:
  dir: /home/ivan/Desktop/RecSys

# -------------------------
# Directories
# -------------------------
dirs:
  raw_data: ${data.base.dir}/raw_data
  processed_data: ${data.base.dir}/dataset
  unique_user_data: ${data.base.dir}/unique_user_data
  graphs: ${data.base.dir}/pt_graphs

# -------------------------
# Files
# -------------------------
files:
  raw_mus_file: ${data.dirs.raw_data}/mus_instr.json
  raw_metadata_file: ${data.dirs.raw_data}/meta_instr.json
  dataset_file: ${data.dirs.processed_data}/dataset.parquet
  metadata_file: ${data.dirs.processed_data}/metadata.parquet

dataset:
  eval_ratio: 0.2
  max_name_len: 10
  max_desc_len: 15
  model_name: "all-MiniLM-L6-v2"
  words_fields: ["product_name", "product_info", "comment"]
  user_field: "customer_id"
  batch_size: 64
  test_size: 0.2
